{
  "name": "agriconnect",
  "version": "1.0.0",
  "description": "A full-stack application for farmers to access weather information, crop advisory, market prices, and government schemes",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "i18next": "^25.1.2",
    "lucide-react": "^0.509.0",
    "react-i18next": "^15.5.1",
    "react-quill": "^2.0.0"
  }
}
